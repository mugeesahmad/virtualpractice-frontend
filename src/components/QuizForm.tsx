import {
  Select,
  SelectTrigger,
  SelectValue,
  SelectItem,
  SelectContent,
} from "./ui/select";
import { Input } from "./ui/input";
import { Button } from "./ui/button";
import { ChangeEvent, useRef, useState } from "react";

const QuizForm = ({ courses = [] }: { courses: string[] }) => {
  
  }

  return (
    <>
     
    </>
  );
};

export default QuizForm;
